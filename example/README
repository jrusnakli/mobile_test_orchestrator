Instruction to run entrypoint script

--entrypoint.py file runs as an independent file
--entrypoint.py calls the pre-packed and pre-installed “orchestrator” lib in python3.7 lib 
to install “orchestrator” lib to python3.7 lib in venv:

Run: $ python3 -m pip install --index-url https://test.pypi.org/simple/ --no-deps androidtestorchestratorwithsupport

--To run entrypoint.py manually for Android espresso test, pass arguments of app.apk path, test_app.apk path and device path from command line.

Example command line:
python entrypoint.py "/mobile_test_orchestrator/testsupportapps/TestButlerTestApp/app/build/outputs/apk/debug/app-debug.apk"
"/mobile_test_orchestrator/testsupportapps/TestButlerTestApp/app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk" "emulator-5554"

-- To run entrypoint.py automatically through Azure custom script extension agent see details in (link) Section 5
